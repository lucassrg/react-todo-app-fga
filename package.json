{
  "name": "react-todo-app-fga",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "if-env TYPE=server && (cd server && npm install) || (cd client && npm install)",
    "server": "node ./server/server.js",
    "client": "cd client && npm start"
  },
  "dependencies": {
    "autoprefixer": "^10.4.17",
    "flowbite": "^2.3.0",
    "if-env": "^1.0.4",
    "postcss": "^8.4.35",
    "tailwindcss": "^3.4.1"
  }
}
